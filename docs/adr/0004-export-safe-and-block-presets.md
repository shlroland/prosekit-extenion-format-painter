# Export safe and block format painter presets

The extension exports `defaultFormatPainterOptions` for safe inline mode and `blockFormatPainterOptions` for common block formatting. This gives applications a named conservative default while making structural copying an explicit opt-in rather than an undocumented collection of options.
