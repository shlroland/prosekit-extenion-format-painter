# Report unsupported styles per target

An unsupported-style callback is emitted once for each failed style and application target, including that target's document range. This lets a multi-target application succeed where possible while allowing hosts to identify and communicate individual failures.
