# Use stable unsupported-style reasons

The unsupported-style callback reports an exported finite reason instead of arbitrary prose. Applications can use these reasons for reliable fallback behavior and user messaging without parsing implementation-defined strings.
