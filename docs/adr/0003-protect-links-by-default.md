# Protect links by default

Safe inline mode treats `link` as its only built-in protected mark. Links identify destinations rather than visual presentation, while collaboration and annotation marks remain application-specific and must be protected explicitly through configuration.
