# Retain one-shot painter after an unsuccessful application

A one-shot format painter clears only after a successful application. When no sampled format can be applied to the target, it remains active and reports unsupported styles so the user can choose another target without resampling.
