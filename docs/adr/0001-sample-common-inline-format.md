# Sample common inline format from mixed selections

A source selection can contain different inline formats. The format painter captures only the inline format common to the whole selection, rather than the format at the selection start, so its result is independent of selection direction and does not copy arbitrary partial formatting.
