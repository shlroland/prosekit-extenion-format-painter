# Scope block preset to ProseKit Basic

`blockFormatPainterOptions` targets the common node and attribute names supplied by ProseKit Basic, with text alignment when that extension is enabled. Other schemas use explicit custom options rather than relying on an implied generic block schema.
