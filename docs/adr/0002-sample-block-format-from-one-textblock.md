# Sample block format from one textblock only

Block format is captured only when the source selection is wholly inside one textblock. A cross-block source selection captures common inline format only, which prevents a result from depending on the selection start and avoids implying that one block represents a heterogeneous source range.
